{
  "name": "chair-event-system",
  "private": true,
  "version": "1.0.0",
  "description": "Chair Event System — executive-event invitation, RSVP, and relationship management.",
  "workspaces": [
    "apps/web",
    "apps/worker"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "worker": "npm run dev --workspace apps/worker",
    "build": "npm run build --workspace apps/web",
    "seed:demo": "npm run seed:demo --workspace apps/web",
    "reset:demo": "npm run reset:demo --workspace apps/web",
    "provision:tenant": "npm run provision:tenant --workspace apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
